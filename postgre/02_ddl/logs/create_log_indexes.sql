-- ---------------------------------------------------
-- LOG TABLE INDEX CREATION
-- ---------------------------------------------------

CREATE INDEX idx_log_stock_batch_id_batch
ON tb_log_stock_batch (id_batch);
